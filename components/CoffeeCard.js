import { Text, View, Image } from "react-native";

const CoffeeCard = () => {
  return (
    <View
      style={{
        borderRadius: 40,
        backgroundColor: "#EDA800",
        height: 350,
        width: 250,
      }}
    >
      <View className="flex-row justify-center -mt-14"></View>
    </View>
  );
};

export default CoffeeCard;
